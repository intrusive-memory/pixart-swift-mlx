name: Tests

on:
  pull_request:
    branches: [main, development]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test-macos:
    name: Test on macOS
    runs-on: macos-26

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Build PixArtBackbone
        run: |
          xcodebuild build \
            -scheme PixArtBackbone \
            -destination 'platform=macOS,arch=arm64'

      - name: Build PixArtCLI
        run: |
          xcodebuild build \
            -scheme PixArtCLI \
            -destination 'platform=macOS,arch=arm64'

      - name: Test
        run: |
          xcodebuild test \
            -scheme pixart-swift-mlx-Package \
            -destination 'platform=macOS,arch=arm64'

  test-ios:
    name: Test on iOS Simulator
    runs-on: macos-26

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Show Swift version
        run: swift --version

      - name: Build for iOS
        run: |
          xcodebuild build-for-testing \
            -scheme pixart-swift-mlx-Package \
            -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.1'

      - name: Test on iOS
        run: |
          xcodebuild test-without-building \
            -scheme pixart-swift-mlx-Package \
            -destination 'platform=iOS Simulator,name=iPhone 17,OS=26.1'
